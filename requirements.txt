fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.7.0
pydantic-settings>=2.3.0
# v3.9.17: pin widened to allow 1.84.x. The v1.84.0 "breaking changes"
# were evaluated (v3.9.17 P4 eval) and are ALL LiteLLM Proxy-server
# features — master-key propagation, pass-through-endpoint auth,
# clientside-credential handling, onboarding flow, CLI SSO. We use
# litellm strictly as a Python library (acompletion / exception
# classes / streaming-chunk parsing / tool calls); the upstream notes
# explicitly state those interfaces are unchanged. Empirical check:
# 1907/1907 unit tests pass on 1.84.0. Ceiling stays at <1.85.0 so a
# future major bump gets the same deliberate evaluation.
litellm>=1.85.2,<1.87.0
# v4.4.30 (2026-05-29) — bumped from <1.85.0 to address 3 critical CVEs
# in the 1.83.0–<1.85.0 range: GHSA-46cm-pfwv-cgf8 (SSTI in /completions),
# GHSA-gppg-gqw8-wh9g (RCE via unsafe eval), GHSA-jjhc-v7c2-5hh6 (auth
# bypass via OIDC cache key collision — we don't use litellm's OIDC,
# but coverage is cleaner with the fixed version). Pin floor at 1.85.2
# (the latest 1.85.x at the time of the bump) and ceiling at <1.87.0
# for safety.
sqlalchemy>=2.0.0
alembic>=1.13.0
aiosqlite>=0.20.0
asyncpg>=0.29.0
redis[hiredis]>=5.0.0
bcrypt>=4.0.0
python-jose[cryptography]>=3.3.0
python-multipart>=0.0.9
httpx>=0.27.0
python-dotenv>=1.0.0
structlog>=24.0.0
tenacity>=8.3.0
anyio>=4.4.0
prometheus-client>=0.20.0
opentelemetry-api>=1.25.0
opentelemetry-sdk>=1.25.0
opentelemetry-exporter-otlp-proto-http>=1.25.0
redisvl>=0.4.0
http-sfv>=0.9.9
jsonschema>=4.20.0

# v5.6.0 — Excel reader for proxy-injected read_xlsx_to_markdown tool
openpyxl>=3.1.0

# v5.7.0 — MCP aggregation endpoint
mcp>=1.27,<2

# v5.7.1 — markitdown: DOCX/PDF/PPTX/HTML/EPUB → markdown for MCP tool
markitdown>=0.1.0

# v5.23 — local accelerator RAM/swap probe (Windows + Linux). Fail-soft
# if missing; probe.py also has ctypes /proc fallbacks.
psutil>=5.9.0
