# v5.0.0 — Per-file impact map

Generated 2026-06-03 by codebase survey. Companion document to `5.0-compliance-design.md`. Cross-reference: each section here maps to the corresponding §3-§10 in the spec.

## 1. Data model

### `app/models/db_apikey.py` — 122 LOC — RISK: medium
Add to `ApiKey` class (after line 78, before `class ApiKeyAiReview`):
- `blocked_companies = Column(JSON, nullable=True)` — list[str] of company IDs (decision 1, 13)
- `allowed_paths = Column(JSON, nullable=True)` — list[str], null = unrestricted (decision 13, 21)
- `debug_echo_enabled = Column(Boolean, default=False)` — decision 13

Test impact: `tests/unit/test_v4418_apikey_sync_coverage.py`, `test_v4425_apikey_insert_field_coverage.py`, `test_v4420_apikey_lww.py` — all enumerate sync field list and will fail until the cluster-sync coverage tests pick up the new columns.

### `app/models/db_provider.py` — 467 LOC — RISK: low
Add to `Provider` class (after line 103, near other `usage_*` cols):
- `owner_company = Column(String, nullable=True)` — auto-derived (decision 17)

Auto-derivation hook: write into `app/api/providers.py` on create/update; map lives in new `app/compliance/company_map.py`. Provider model itself only gains the column.

### `app/models/db_caller_memory.py` — 68 LOC — RISK: low
- `CallerMemory` already has `source_provider_id` (line 37). Add `source_company = Column(String, nullable=True)` after line 37 (decision 18).
- `CallerMemoryMarker` already has `last_known_provider_id` (line 65). Add `source_company = Column(String, nullable=True)` after line 65.

Test impact: `tests/unit/test_v387_caller_memory_phase2.py`, `test_v388_caller_memory_store.py`, `test_v389_memory_injection.py`, `test_v390_memory_extract.py` — they round-trip the columns.

### `app/models/db_activity.py` — 49 LOC — RISK: low
`ActivityLog` (line 39) needs no new columns — compliance fields go in `event_meta` JSON (decision 9, "activity_log enrichment"). NEW tables go in a new model file (next section).

### NEW: `app/models/db_compliance.py` — RISK: medium
New table classes for decision 16:
- `class ComplianceEvent(Base)` — schema in spec §3.2
- `class CompliancePolicyChange(Base)` — schema in spec §3.2
- `class ComplianceAuditChain(Base)` — for daily integrity hash (decision 10): `id`, `day`, `row_count`, `chain_hash` (sha256 of prior day's hash + sorted ComplianceEvent ids+content for the day).

Register in `app/models/__init__.py` and `app/models/db.py`.

### `app/models/db.py` — RISK: low
Re-export new models: `from app.models.db_compliance import ComplianceEvent, CompliancePolicyChange, ComplianceAuditChain`.

### `app/models/database.py` — 540 LOC — RISK: high (migration path)
Append to ALTER list (the `for stmt in [...]` block ending line 403):
```python
"ALTER TABLE api_keys ADD COLUMN blocked_companies TEXT",
"ALTER TABLE api_keys ADD COLUMN allowed_paths TEXT",
"ALTER TABLE api_keys ADD COLUMN debug_echo_enabled BOOLEAN DEFAULT 0",
"ALTER TABLE providers ADD COLUMN owner_company TEXT",
"ALTER TABLE caller_memory ADD COLUMN source_company TEXT",
"ALTER TABLE caller_memory_marker ADD COLUMN source_company TEXT",
```

Add a v5.0.0 backfill block AFTER the v4.4.27 unique-index block (line 504): one-shot SQL to populate `caller_memory.source_company` and `caller_memory_marker.source_company` via JOIN on providers. Gate on `SystemSetting.compliance_backfill_applied`.

Add indices: `ix_compliance_events_api_key_created`, `ix_compliance_events_event_type`.

## 2. Compliance core (NEW)

All NEW files under `app/compliance/`.

- `__init__.py` — 0 LOC — public exports
- `company_map.py` — KNOWN_COMPANIES + provider_type_to_company + model_family_to_company
- `policy.py` — get_effective_blocklist (cached), is_company_banned, filter_providers, ComplianceNoSubstituteError
- `ua_detect.py` — narrow patterns + detect_client_company
- `disclosure.py` — compliance_headers + sse_prelude_anthropic/openai_inject + respects_opt_out
- `audit.py` — emit_event, emit_policy_change, compute_daily_integrity_hash, 7-year retention purge

## 3. Router + dispatch

### `app/routing/router.py` — 800 LOC — RISK: high (critical path)
- Add `blocked_companies` param to `select_provider()` at line 209-224. Resolve from `api_key_id` if omitted.
- INSERT compliance hard-pre-filter at line 354 — BEFORE the `exclude_provider_id` block.
- If filter empties: raise `ComplianceNoSubstituteError`.
- The EXISTING cross-family-fallback path at line 442-455 handles the substitution.
- `RouteResult` gains `compliance_substituted`, `compliance_blocked_company`, `compliance_served_company`.

### `app/routing/litellm_binding.py` — 287 LOC — RISK: low
No code changes. Reuse `_model_family_provider_types` at line 105-145 from `app/compliance/company_map.py::model_family_to_company`.

### `app/api/messages.py` — 861 LOC — RISK: high
- UA pre-check after `verify_api_key` at line 81 (decision 22): if banned UA detected, 451 immediately + emit_event.
- After `select_provider_with_503` at line 183: merge compliance headers if `route.compliance_substituted`.
- Pass `compliance_substituted` flag into `_stream_anthropic` invocations.
- Allowed-paths defensive check.

### `app/api/completions.py` — 742 LOC — RISK: high
Mirror messages.py — UA check at line 78, disclosure headers at line 169, pass flag into `_stream_openai`.

### `app/api/_messages_dispatch.py` — 386 LOC — RISK: medium
- `_select_excluding()` line 39 — propagate blocked_companies
- `dispatch_claude_oauth_chain()` line 74, `try_cascade_dispatch()` line 272 — nested `select_provider` calls respect the blocklist

### `app/api/_messages_streaming.py` — 483 LOC — RISK: medium
- `_stream_anthropic()` line 227: prepend SSE prelude before message_start (line 261-266). Decision 14+28.
- `_stream_cot_anthropic()` line 120: same prelude.

### `app/api/_completions_streaming.py` — 233 LOC — RISK: medium
- `_stream_openai()`: inject `compliance_substitution` key into first data frame.
- `_stream_cot_openai()` line 30: same.

### `app/monitoring/helpers.py` — 690 LOC — RISK: medium
- `record_outcome()` line 469: add `compliance_substituted`, `blocked_company`, `served_company` kwargs.
- Inject into `event_meta` via `_build_event_meta_base()` at line 201.
- Call `compliance.audit.emit_event()` when compliance fields are set.

### `app/api/_request_pipeline.py` — RISK: medium
- `build_base_response_headers()` line 195: merge compliance headers when `route.compliance_substituted` — extend the existing `X-Substituted-From/To` block at lines 226-233.
- `select_provider_with_503()`: catch `ComplianceNoSubstituteError` → 503.

## 4. Cluster sync

### `app/cluster/manager.py` — 590 LOC — RISK: high
- `_build_sync_payload()` line 153: extend api_keys dict (lines 163-191) with new fields. Extend providers dict (lines 194-241) with `owner_company`.
- Add new payload sections: `compliance_events`, `compliance_policy_changes` (decision 16).
- **NEW: `push_policy_change_with_quorum`** function — ~80 LOC of new code.

### `app/cluster/sync.py` — 915 LOC — RISK: high
- api_keys merge block lines 71-205: extend UPDATE branch with new fields. Invalidate blocklist cache on changes.
- INSERT branch line 181-202: add three fields to `ApiKey(...)` constructor.
- providers merge block: add `owner_company` to UPDATE + INSERT paths.
- Wire up `_apply_compliance_events` + `_apply_compliance_policy_changes` in `apply_sync()` at line 875.

### `app/cluster/sync_handlers.py` — 386 LOC — RISK: medium
Add two append-only handlers (mirror `_apply_blocked_ips` at line 36, with `.limit(1)` guard from BUG-080):
- `async def _apply_compliance_events(db, rows)`
- `async def _apply_compliance_policy_changes(db, rows)`

Register in `__all__` at lines 908-915.

## 5. Cache + memory

### `app/cache/keys.py` — RISK: medium
- `build_namespace()` line 51-74: namespace already includes `tenant_id`. Recommend: keep namespace shape; apply company filter at query/check time inside `semantic.py` (cheaper).

### `app/cache/semantic.py` — 184 LOC — RISK: high
- `check()` line 118: take `blocked_companies` kwarg. After query at line 137, filter results. Add `source_company` to `return_fields` at line 133.
- `store()` line 152: take `source_company` kwarg; add to hashed payload at line 164-170.
- `_INDEX_SCHEMA` line 20: add `{"name": "source_company", "type": "tag"}`.
- **WARNING**: Redis index schema migration needed. First-boot detects mismatch and rebuilds index (loses cached rows).

### `app/cache/middleware.py` — RISK: medium
- Caller of `cache.check()` line 123 and `cache.store()` line 143: wire through `blocked_companies` + `source_company`.

### `app/memory/store.py` — 316 LOC — RISK: high
- `get()` line 79: take `blocked_companies` kwarg. After loading CallerMemory row (line 113-130), filter NULL or in-banned. Also filter Redis-cached entries (line 98-104).
- `put()` line 147: take `source_company` kwarg, write through to `CallerMemory.source_company` (line 184-191) and existing-update branch (line 191-202). Mirror on `CallerMemoryMarker` (line 211-232).
- Redis payload deserialization (line 98-105): include `source_company`.

### `app/memory/inject.py` — 170 LOC — RISK: medium
- `maybe_inject_memory()` line 66: resolve `blocked_companies` from api_key, pass into `get()` at line 90.

### `app/memory/extract.py` — 230 LOC — RISK: medium
- `maybe_extract_memory_writes()` line 36: resolve `source_company` from `source_provider_id`, pass to `put()` calls (lines 95, 148, 167, 189, 210).

### `app/memory/flush.py` — RISK: low
- Check flush calls touching marker; propagate `source_company` if needed.

## 6. API endpoints + admin UI

### `app/api/apikeys.py` — 358 LOC — RISK: medium
- `KeyCreate` line 22: add three fields.
- `KeyUpdate` line 38: same (Optional defaults).
- `create_key()` line 71: persist in `ApiKey(...)` constructor (lines 78-93).
- `update_key()` line 103: write conditionally (mirror lines 111-141). Emit `compliance.audit.emit_policy_change` on changes. Trigger quorum-sync for these changes.
- `_serialize()` line 335: add the three fields.

### `app/api/settings_api.py` — 146 LOC — RISK: medium
- `get_settings()` line 23 / `put_settings()` line 62: handled automatically when keys are in `config_runtime.SCHEMA`.
- Emit `CompliancePolicyChange` row on changes to compliance system settings.

### `app/config_runtime.py` — 542 LOC — RISK: low
Add to `SCHEMA` (group: "Compliance"):
- `compliance_enabled` (bool, default False)
- `compliance_system_blocked_companies` (list, JSON-encoded)
- `compliance_audit_retention_days` (int, default 2555)
- `compliance_ua_block_enabled` (bool, default True)
- `compliance_sse_prelude_enabled` (bool, default True)

### `app/config.py` — 451 LOC — RISK: low
Add corresponding `Settings` attributes (5 fields).

### NEW: `app/api/compliance.py` — RISK: medium
Endpoints (decision 15):
- `GET /api/debug/echo-client` + `POST` — sandbox, gated on `key.debug_echo_enabled`
- `GET /api/admin/cluster/compliance-ready` — admin
- `GET /api/admin/compliance-events?format=csv` — admin
- `GET /api/me/compliance` — user-visible

### `app/main.py` — 696 LOC — RISK: medium
- Register new compliance router at line ~520.
- Register `allowed_paths` middleware after `ip_block_middleware`.

### `app/auth/keys.py` — 172 LOC — RISK: high
- `verify_api_key()` line 58: add `allowed_paths` check after rate-limit (line 89-104). Recommended: dedicated middleware (next item).
- `ApiKeyRecord` dataclass line 38: add `blocked_companies`, `allowed_paths`, `debug_echo_enabled`.

### NEW: `app/middleware/allowed_paths.py` — RISK: medium
Mirror `app/middleware/ip_block.py` shape. Load `ApiKey.allowed_paths` (cached), 403 on mismatch.

### Frontend `frontend/src/pages/APIKeysPage.tsx` — 669 LOC — RISK: medium
UI for three new fields (decision 25):
- Blocked companies multi-select (10 + custom-add)
- Allowed paths list editor
- Debug echo enabled toggle

### Frontend `frontend/src/pages/SettingsPage.tsx` — 289 LOC — RISK: low
Comes "for free" once `config_runtime.SCHEMA` has Compliance group. Extend `DynamicSettingsPanel` with `"company_multiselect"` widget type.

### NEW Frontend `frontend/src/pages/CompliancePage.tsx` — RISK: medium
Admin: list compliance_events with filters + CSV export.

### NEW Frontend `frontend/src/pages/MyCompliancePage.tsx` — RISK: low
User: read `/api/me/compliance`.

### `frontend/src/App.tsx` — 69 LOC — RISK: low
Add two routes after line 49.

## DELETIONS / NO-OPS (UNAFFECTED)

- `app/routing/lmrh/` — runs AFTER compliance pre-filter; no policy concern.
- `app/routing/circuit_breaker.py` — provider health gate; orthogonal.
- `app/routing/canonical.py`, `capability_inference.py`, `classifier.py`, `external_rotation.py`, `hedging.py`, `shadow.py`, `cascade.py`, `retry.py` — run inside `select_provider` after the new pre-filter has narrowed the pool.
- `app/api/embeddings.py`, `app/api/models.py`, `app/api/runs.py` — not in chat critical path.
- `app/api/aliases.py`, `model_slug.py` — alias resolution acts on resolved model later.
- `app/auth/sso.py`, `rbac.py`, `admin.py`, `cluster_hmac.py` — auth shape unchanged.
- `app/budget/` — single-bucket charge (decision 6).
- `app/observability/`, `app/monitoring/notifications.py`, `app/monitoring/keepalive.py` — telemetry surfaces; no code changes.
- `app/providers/` — provider-specific upstream clients; never see compliance.
- `app/runs/`, `app/api/runs.py` — async run engine; out of spec.
- `app/airi/` — separate chat router; out of scope.
- `app/cot/` — CoT pipeline; runs after route is chosen.
- `alembic/` — empty (0 files); live migration is `database.py::init_db`, NOT alembic.
- `grok_bridge/`, `whisper_bridge/`, `sdk/`, `v1-reference/`, `tools/`, `scripts/` — sidecars + reference; unaffected.

## Summary

- Files to edit: 22 (excluding new files)
- New files: 10 (`app/compliance/__init__.py`, `company_map.py`, `policy.py`, `ua_detect.py`, `disclosure.py`, `audit.py`; `app/models/db_compliance.py`; `app/api/compliance.py`; `app/middleware/allowed_paths.py`; 3 frontend pages)
- New DB tables: 3 (`compliance_events`, `compliance_policy_changes`, `compliance_audit_chain`)
- New columns: 6 (`api_keys.blocked_companies/allowed_paths/debug_echo_enabled`; `providers.owner_company`; `caller_memory.source_company`; `caller_memory_marker.source_company`)
- New settings keys: 5
- Tests likely to break: ~14 (router/sync/memory/cache tests enumerating field sets)
- Highest-risk edits: `app/routing/router.py:354` (pre-filter), `app/models/database.py:402` (migration), `app/cluster/sync.py:71-202` (sync field coverage), `app/cluster/manager.py:503` (NEW quorum push), `app/cache/semantic.py:20` (Redis schema migration), `app/auth/keys.py:58` (allowed_paths gate).
