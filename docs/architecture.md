# llm-proxy-v2 — architecture

**Moved.** The canonical architecture document is
[`../architecture.md`](../architecture.md) at the repository root.

This `docs/architecture.md` was a second copy that drifted out of date
(it was pinned at v3.7.13 while the root document tracked the current
release). The v3.10.9 maintainability refactor collapsed it to this
pointer so there is exactly one architecture document.

Do not edit this file — update the root `architecture.md` instead.
See also [`../design.md`](../design.md) (module-boundary contract) and
[`../refactor-log.md`](../refactor-log.md).
